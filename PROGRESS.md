# سجل تقدم الحزم — Tashjir Packages Progress

> الغموض يُحل بالأكثر أمانًا ويُوثق هنا (القاعدة ٨). الأرقام بالعربية في الواجهة فقط؛ هنا أرقام لاتينية لسهولة المراجعة.

| الحزمة | المرحلة | المتطلبات | الحالة | ملاحظات |
|---|---|---|---|---|
| 07 | PH6 | FR-ED-10 (+ DM-08 · DM-12 تفصيلًا) | ✅ منجزة 2026-09-13 | التعميم متعدد الأنواع + التحرير المحلي المستقل. 520 اختبارًا أخضر + typecheck. |

## الحزمة 07 — التفاصيل

### افتراضات البدء (تم التحقق 2026-09-13)
- الحزمة 06 (المعالج متعدد الأنواع): **منجزة جزئيًا**. `SmartCreateWizard` + `buildSmartCreateBatch` ينشئان محليًا كل نوع كيانًا مستقلًا برتبته ومعرّف دفعة، والتعميم على المصحف ينشئ قاعدة لكل نوع برتبة `index+1` — لكن **بلا `createBatchId` على القواعد العامة** وبلا حفظ دفعي ذري يحفظ ترتيب الكتلة. سأُنفذ الحد الأدنى الناقص ضمن هذه الحزمة (دالة `saveGlobalRuleBatch` + حقل `createBatchId`).
- الحزمة 01 (`RuleOccurrence` + `localOverride`): **النموذج v8 يقبل `localOverride` لكن مخزن الاستثناءات الفعلي (`rule-occurrences-store`) لا يدعم إلا درجة القوة والرتبة والحذف**. سأُوسع المخزن بترقيع محلي كامل (نص/وصف/نطاق/رتبة/درجة) مع توافق رجعي.
- السلوك القائم: مواضع مشتقة بلا نسخ + حالات (APPLIED/DELETED/CONFIRMED) + سجل `occurrenceLog` — يُحافَظ عليه ويُوسع (حقل `changes` قبل/بعد + `source`).

### قرارات الأمان (الأكثر أمانًا عند الغموض)
1. **«تحقيق» ليست فئة جديدة**: فئات `VariantCategory` الست ثابتة. مثال (تحقيق/أصول/فرش) يُقرأ كثلاثة أنواع/قواعد برتب صريحة 1/2/3 (العنوان/الحكم يسمى تحقيقًا) — لا نضيف فئة ولا نكسر `CATEGORY_LABELS`. الرتبة رقم صريح لا اسم.
2. **لا نسخ آلاف المستندات**: التجاوز طبقة فوق المشتق (`localOverride.patch`) والعرض = المشتق + الدمج. الحذف المحلي `state=DELETED` فقط.
3. **التجاوز المحلي يغلب تعديل القاعدة اللاحق** (سلم الخصوصية)، مع شارة «متجاوز محليًا».
4. **التراجع**: يغطي المستند + الاستثناءات + القواعد معًا في خطوة واحدة (لقطة شاملة) حتى لا يبقى أثر معلق.
5. **معرّف الموضع المشتق مستقر** (`global:<ruleId>:<ayahKey>:…`) ولا يتغير بالترقيع (الترقيع لا يمس المدى الحرفي).

### التتبع (P0 ثم P1 ثم P2)
- P0: T1 ✅ (ترتيب صريح + شارات) ← T2 ✅ (تعديل/حذف محلي + إلغاء التجاوز + نسخ مستقل + علاقة/دمج عبر الموجود) ← T3 ✅ (حماية الأم + تحذير كمي + Undo) ← T4 ✅ (16 اختبارًا جديدًا).
- المتبقي: لا شيء — كل القبولات الأربعة متحققة.

### سجل التنفيذ
- 2026-09-13: بدء الحزمة. فحص `rule-occurrences-store` و`global-rules-store` و`global-rule-engine` و`ordering` و`editor-store` و`VariantsPanel` و`RulesIndexDialog`.
- 2026-09-13: الأساس — `saveGlobalRuleBatch` + `createBatchId`، و`LocalOverridePatch` (دمج + تحرير بـundefined) مع `changes` قبل/بعد، وتطبيق الترقيع في `variantFromGlobalMatch` مع `hasLocalOverride` وثبات المعرّف، و`matchFromDerivedVariant` بآخر فاصل.
- 2026-09-13: الرتبة الصريحة أولًا في `branch-engine` (حمل `orderRank` في `TashjeerBranch`) وفي `editor-store.compareVariants` (ثم المعرّف بدل العنوان)؛ الكلاسيكي كان سليمًا أصلًا.
- 2026-09-13: التراجع الموحد — لقطات `EditorHistoryEntry` (مستند+استثناءات+قواعد) و`transactExternal`، وإصلاح علة الالتقاط-بعد-الاستعادة في undo/redo (كشفتها الاختبارات الجديدة).
- 2026-09-13: الواجهة — `LocalOverrideEditor` + أزرار المواضع المشتقة في لوحة الترتيب (تحرير/حذف/إلغاء/نسخ/إرجاع) بالأرقام العربية، وتغليف كل إجراءات القواعد والمواضع بالتراجع، وتحذيرات الأمّ الكمية، وتراجع مباشر في صفحة المكتبة.
- 2026-09-13: `tests/package07-local-independence.test.ts` (16 اختبارًا: T1=4، T2=7، T3=2، T4=3). الإجمالي 520 أخضر + typecheck. التوثيق: `EDITOR.md` §6.8 و`DATA.md` §5.1.
