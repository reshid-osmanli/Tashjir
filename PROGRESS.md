# سجل التقدم — مشروع التشجير

| الحزمة | المرحلة | الحالة | ملاحظات |
|---|---|---|---|
| 01 — النموذج الموحّد (Correction/AuditLog/GlobalRule/RuleOccurrence) | PH0 | ✅ مكتملة | `src/lib/tashjeer/model/v8.ts` + اختبارات model-v8 |
| 02 — استوديو المحرك (Rule Builder + Why/Trace عبر Decision API) | PH1 | ✅ مكتملة | `/studio` + `src/lib/tashjeer/decision/*` |
| 03 — التحديد الموحد (Selection Context) | PH1 | ✅ مكتملة | `selection` في مخزن المحرر + `selection-context.ts` |
| 04 — الحافظة (Clipboard) | PH2 | ✅ مكتملة | copySelection/pasteSelection + اختبارات |
| 05 — التعميم المحلي | PH2 | ✅ مكتملة | Generalize → GlobalRule + RuleOccurrenceReview |
| 06 — الوقف والوصل | PH3 | ✅ مكتملة | Boundaries + ReadingWindow + ممنوع الوصل |
| 07 — التجاوز المحلي لمواضع القواعد | PH4 | ✅ مكتملة | rule-occurrences-store (overrides + log) |
| 08 — قواعد التنافي والعلاقات | PH4 | ✅ مكتملة | RelationsPanel + resolveRelationExclusion |
| 09 — القواعد في قائمة الاختلافات + التتبع الموحد + حلقة التعلم | PH8 | ✅ مكتملة | T1–T6 كلها (T4 بالدرجة P1) — انظر التفاصيل أدناه |

## الحزمة 09 — تفاصيل الإنجاز

### T1 (P0) — القواعد كعناصر مستقلة في قائمة الاختلافات (FR-ED-15) ✅
- `GlobalRuleListItem` في قائمة الاختلافات (VariantsPanel): القاعدة عنصر واحد
  مستقل قابل للتحديد (via `selectRule`، kind `RULE`) والتوسيع لمواضعه المشتقة
  بحالاتها (مطبَّق/معتمد/مخصص/محذوف موضعيا) — بلا نسخ.
- تحرير من القائمة أو من لوحة الخصائص (`GlobalRuleMetaEditor`) ينعكس على كل
  المواضع غير المتجاوزة محليا؛ إيقاف/تفعيل من نفس العنصر.
- نفس الـID في كل الواجهات: القائمة، التتبع، المحرر (المواضع المشتقة)،
  `/variants` وفهرس القواعد المدمج، الاستوديو (عبر `?rule=` للسياسات).
- رابط عميق `/editor?ayah=...&rule=global-...` يحدد القاعدة نفسها (مميّز عن
  معرّف قاعدة الاستوديو المخصص لأثر «لماذا؟»).

### T2 (P0) — التتبع الموحّد (FR-ED-16) ✅
- أحداث الحزم 04–08 اكتملت في `editLog`: نقل وجه/إعادة ترتيب أوجه
  (`ALTERNATIVE_ORDER`)، دمج/فصل روابط (قائم)، حذف جماعي (قائم)، لصق (قائم)،
  تجاوز محلي (قائم عبر مخزن الاستثناءات)، وعلامات وقف/وصل (`BOUNDARY`) ونافذة
  القراءة (`WINDOW`) — كلها بفئة WAQF حيث يناسب.
- ثلاثية (Engine/Editor/Final) مع **السبب والمصدر** من آخر سطر مسجّل على الموضع.
- كل سطر «فتح في المحرر» برابط صحيح: الاختلاف/الموضع المشتق محددًا في
  التحديد الموحد؛ لسطر الحدث العام الآية نفسها.
- Decision Trace بجوار الفروق (قائم، `resolveDifference`).

### T3 (P0) — Reference Validation (FR-ES-12.1) ✅
- `lib/tashjeer/reference-validation.ts` (نقي): تصنيف حتمي لكل موضع
  `Correct/Wrong/Missing/Extra/Conflict` مقارنة بلقطة المحرك المحفوظة،
  الاستثناءات المحلية، والروابط اليدوية المتعارضة (عبر
  `resolveRelationExclusion` — P-07).
- وضع في `/tracking` مع ملخص كمي + تصفية بكل تصنيف + فتح الموضع في المحرر.
- اختبارات: عينة مصطنعة خماسية تُصنَّف تصنيفًا صحيحا (AC-07) + حتمية.

### T4 (P1) — مستكشف أخطاء المحرك (FR-ES-12.2) ✅
- `lib/tashjeer/engine-errors.ts` (نقي): تجميع حتمي (فئة القاعدة × فئة القرار
  × السياق × المصدر) بلا تقنيات احتمالية، مرتب تنازليا بالعدد.
- وضع «أخطاء المحرك» في `/tracking`: من كل نمط تُفتح المواضع المتأثرة في
  المحرر (تحديد موحد).

### T5 (P0) — القواعد المرشحة + Create Rule from Correction (FR-ES-12.3/12.4) ✅
- `readCorrectionContexts` (التتبع): يجمع تصحيحات المستندات (A/B) قراءة صرفة.
- قسم «قواعد مرشحة من تصحيحاتك المتكررة» في استوديو المرشحين: نمط تكرر ≥ 3
  يظهر ببطاقة تعرض عدد التصحيحات المشابهة، النمط المشترك، والأثر المتوقع —
  وزر «مراجعة وإنشاء القاعدة» (Review ← Create Rule). لا إنشاء تلقائي أبدا
  (P-06): القاعدة تبقى DRAFT.
- «أنشئ قاعدة من التصحيح»: زر في المحرر (لوحة الخصائص، عند وجود ثلاثية) وزر
  في صف التتبع، يفتحان الاستوديو مُعبأين من سياق التصحيح (نوع/قرار A/قرار
  B/الموضع) مع «افتح التصحيح في المحرر» للعودة. لا يُنشأ شيء قبل الحفظ اليدوي.
- `CandidatePattern` يحمل `engineMerged` لتعبئة دقيقة من النمط.

### T6 (P0) — الربط الثنائي محرر↔استوديو + Why في المحرر (FR-ES-15) ✅
- محرر→استوديو: «في الاستوديو» في لوحة الخصائص يحسب القاعدة الفاعلة عبر
  `resolveDifference` ويفتح `/studio?rule=<id>` (أو قسم القواعد إن لم توجد).
- استوديو→محرر: «المواضع المتأثرة: N» في `/variants` وفهرس القواعد (قائم
  «تتبّع المواضع» + العدد الجديد) وقائمة الاختلافات — واختيار موضع يفتح
  المحرر **محددًا الموضع نفسه** (رابط عميق بالآية + معرّف الموضع المشتق).
- Why في المحرر: القسم «تجاوزته» يعرض القواعد التي خسر الصراع بأولوياتها —
  من خطوات `lost` جديدة في أثر `decideMerge` (مختبرة).
- نفس Selection Context في الاتجاهين (تحديد موحد واحد، لا أنظمة موازية).

### التوثيق والاختبارات
- `docs/ENGINE_STUDIO.md`: حلقة التعلم الكاملة + Create Rule from Correction +
  التحقق من المرجع + مستكشف الأخطاء.
- `docs/EDITOR.md`: §6.8 (القواعد في القائمة) + §6.9 (التتبع الموحّد) +
  §6.10 (لماذا؟ والربط الثنائي) + جدول الاختبارات المحدّث.
- `README.md`: مخطط حلقة التعلم + بنود المنجز المحدثة.
- اختبارات جديدة: 34 (reference-validation 14، engine-errors 6،
  editor-tracking-events 8، correction-contexts 4، decision-override-trace 2).
- الحالة النهائية (قبل الدمج مع main الجديدة): 538 ناجحا + 2 متجاوز، typecheck
  نظيف، build ناجح.

### الدمج مع main الجديدة (إعادة الأساس)
تحركت main بعد تأسيس هذه الفرع (PR #71) بدون سلف مشترك، فأُعيد تأسيس
الحزمة عليها (`rebase --onto`) وحُلّت التعارضات في: `editor-store.ts`
(حذف علامة الوقف: سجل التعديل + تصفير التحديد معًا)، `VariantsPanel.tsx`
(أُبقيت قائمة تنافذ العمل واستعيدت ميزات main: الإنشاء السريع بالقوالب
والتحديد المتعدد للأوجه وبوابة الحذف الموحّدة)، `resolver.ts` (نموذج
الحسم الجديد في main يصدر خطوات `lost` جاهزة لقسم «تجاوزته» فلا حاجة
لخطوات `OVERRIDDEN` مخصصة)، `EDITOR.md` (إعادة ترقيم أقسامي إلى
6.9/6.10/6.11 بعد أن اشتمل main على §6.8 الخاص بـ FR-ED-10)،
`PROGRESS.md` (main كانت نسخة فارغة).

**إصلاحات ضرورية: ملفات تالفة في main نفسها** (تحقّق: typecheck على main
الصافية يفشل في 3 ملفات) أُصلحت هنا لتخضرّ الشجرة:
- `editor-store.ts`: دوال `selectWord/selectVariant/selectAlternative/
  selectSegment/selectLine` فقدت جسم `set(...)`؛ `deleteAlternative`
  وحذف جماعي للاختلافات فقدت تنفيذهما (أُعيدتا على نمط `deleteItems`
  الذري)؛ `requestDeleteItems` (بوابة الحذف المؤكد الكمي) فقدت كامل
  (أعيد بناؤها بمطابقة اختبارها: العنوان والأثر والترتيب)؛ `undo/redo`
  فقدت سطر `document: restored`؛ واستعادة اللقطة صارت حرفية (بلا إعادة
  توليد خطوط) لمطابقة اختبار التراجع الحتمي.
- `VariantsPanel.tsx` في main كانت JSX معلقة (قائمة بلا `<ul>` و`range`
  غير معرّف) — بُنيت من النسخة العاملة مع ميزات main.
- `SmartCreateWizard.tsx` في main: فرع التعميم في `create` وقسم من
  الاستيرادات وفكّ مخزن المحرر فقدت (أُعيد بناؤها: قواعد ذرية
  `saveGlobalRulesBatch` داخل `transactExternal`).
- `RelationsPanel.tsx`: `LinksList` فقدت `selection/operationNotice/
  mergeRecords/unmerge/confirmLegacy` (أُعيدت عبر `requestUnmergeLines`).
- `HistoryControls.tsx`: تكييف مع اللقطة الموحّدة (مستند + استثناءات + قواعد).

- الحالة النهائية للفرع: **638 ناجحا + 2 متجاوز (61 ملفًا: 47 قائمًا + 5 من
  هذه الحزمة + 9 من الحزم المتلاحقة على main)، typecheck نظيف، build ناجح.**

### افتراضات موثقة (لا أسئلة معلقة)
- «المرجعي» في Reference Validation هو كل ما ثبت بقرار المحرر (Final) —
  الطبقة الموثوقة حاليا (قرار محسوم سلفا في الحزمة).
- الموضع بلا لقطة محرك (بيانات أساسية لم يمسها أحد) يصنّف `Correct` — أأمن من
  اختلاق خطأ؛ التوثيق في صفحة التتبع.
- التجميع الحتمي لأخطاء المحرك: (الفئة × التصنيف × السياق × المصدر)؛ القارئ
  غير متوفر على مستوى الموضع في النموذج الحالي فلا يدخل المفتاح.
- القاعدة المرشحة من نمط متكرر تبنى بالسياق الأول للنمط (نفس سلوك
  `proposeCandidateRule` القائم) وتبقى DRAFT حتى اعتماد المستخدم.
- «التعميم» (Generalize) يخلق كيان القاعدة نفسه القابل للتتبع في الفهرس
  والقائمة؛ لا يغيّر مستند الآية فلا يضيف سطر editLog — القاعدة الجديدة
  ظاهرة في قائمة الاختلافات والفهرس والتتبع بمعرّفها.
