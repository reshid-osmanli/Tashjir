# سجل تقدم الحزم — Tashjir Packages Progress

> هذا السجل بدأ مع الحزمة 08. الحزم 01–07 سُلّمت قبل إنشائه (الكود القائم يشهد
> بمنجزاتها: النموذج v8، قوالب السياق، التحديد الموحد، المعالج الذكي، التعميم).

| الحزمة | المرحلة | الحالة | ملاحظات |
|---|---|---|---|
| 08 — الوقف والوصل والابتداء وممنوع الوصل والعزل (FR-ED-11 · DM-06/07/11 · AC-03) | PH7 | ✅ مكتملة | T1+T3+T2+T4+T5: سياق بحت، قرارات، مخزن، واجهات، تكافؤ /quran، 24 اختبارًا |

## الحزمة 08 — قرارات الأمان الموثقة (الغموض حُسم بالأكثر أمانًا)

1. **نهاية الآية بلا علامة = وقف طبيعي**: اختلاف «وقفا فقط» في نهاية آية غير
   موصولة يظهر افتراضيًا، ويسقط عند وصل الآية بالتالية؛ «وصلا فقط» بالعكس.
2. **الوصل يغلب علامة الوقف التوثيقية عند حدّ الوصل نفسه** (تبقى العلامة في
   البيانات؛ العرض وحده يتبدل)، أما «ممنوع الوصل» فيمنع الوصل أصلًا ولا يُتجاوز.
3. **ممنوع الوصل = وقف إجباري** عند تقييم السياق (لا يمكن الوصل حيث مُنع).
4. **لا توليد تلقائي لعلامات وقف ولا اختلافات** — المحرك يعرض ما أثبته المحقق
   وفق السياق وقواعد الاستوديو المفعّلة (ACTIVE فقط)؛ القواعد المسودة لا تحجب.
5. **الوصل بين مقطعين داخليين يُظهر أحكام «وصلا فقط» المثبتة عند الحدّ**
   ويُسقط أحكام «وقفا فقط» — «التوليد» هنا إظهارٌ سياقي لا اختراع أحكام.

## الحزمة 08 — ما سُلّم

- **T1**: سياق آخر الآية في `variantAppliesToRecitation` + بوابتا المحركين
  (`combination-engine` و`classic-tashjeer` تقبلان `recitation`) + شارة الوضع
  المرئية «⏸ وقف / 🔗 وصل» في المحرر.
- **T2**: `segmentWasl` في النافذة (محفوظ/مطهَّر/مُرحَّل) + تعداد الحدود
  `listJoints` + وصل/فصل المقاطع من اللوحة ولوحة الخصائص.
- **T3**: `resolveConnection` موسّعة (مرجع العلامة + القواعد الحاجبة) +
  `resolveDifferenceContext` + رفض المخزن المسجّل (`lastConnectionRejection`)
  برسالة ومرجع وزر تراجع، ولا تغيير في المستند عند الرفض.
- **T4**: النموذج v8 نفسه موسّع لا مكسور + العلامة في التحديد الموحد (نقرًا من
  اللوحة والقائمة، وقسم خصائص، وفتات سياق) + تكافؤ `/quran` (السياق نفسه عبر
  `useAyahTashjeer`، والمقطع المعزول وعلامات الآيات في البطاقة) + التصدير يحمل
  `segmentWasl` وعلامات الوقف بنطاقها ومصدرها.
- **T5**: `tests/waqf-wasl-context.test.ts` (24 اختبارًا لـAC-03) + تحديث قفل
  شكل `readingWindow` في `import-migration-v8` + الاختبارات والفحص النوعي
  والبناء خضراء.
