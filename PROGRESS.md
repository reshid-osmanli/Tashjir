# سجل تقدم الحزم — Packages Progress

> مصدر الحقيقة الواحد لحالة حزم العمل. يُحدَّث صف الحزمة عند البدء وعند الانتهاء.
> القاعدة: P0 ثم P1 ثم P2، والمتبقي يُسجَّل بمعرفاته وأولويته.

| الحزمة | المرحلة | المتطلبات | الحالة | الملاحظات |
|---|---|---|---|---|
| 01 — النموذج + Resolver | PH4 | DM-01..09 | ✅ منجزة (تحقق من المستودع) | `Difference` بمعرف مستقل + `occurrenceIndex` + علاقة `MUTUALLY_EXCLUSIVE` في `model/v8.ts`؛ `decision/resolver.ts` + `api.ts` |
| 02 — سياسات الدمج والتنافي | PH4 | Merge Matrix | ✅ منجزة (تحقق من المستودع) | `decision/policy.ts` (المصفوفة الافتراضية + قواعد النظام) + `editor-bridge.ts` (`resolveLocusExclusion`/`resolveExclusiveGroups`) |
| 03 — التحديد الموحد | PH4 | FR-ED-02 | ✅ منجزة (تحقق من المستودع) | `selection-context.ts` + حقل `selection` الوحيد في `editor-store.ts` |
| 04 | PH4 | — | ⏸ لم تُفحص في هذه الحزمة | خارج نطاق الحزمة 05 |
| **05 — تعدد الاختلافات المستقلة لنفس القارئ والموضع** | PH4 | **FR-ED-03 (+ DM-09)** | ✅ **منجزة 2026-09-13** | الفهرسة + التنافي من المصفوفة + التركيب + واجهة الموضع + 21 اختبارا؛ 525 خضراء والبناء سليم |
| 06 — معالج الإنشاء الذكي | PH4 | — | ⏸ خارج النطاق | تحسين تجربة فوق أساس 05 |
| 07 — التعميم على المصحف | PH4 | — | ⏸ خارج النطاق | — |

## الحزمة 05 — تفصيل التقدم

- [x] T1 — نموذج التعدد (DM-09): `occurrenceIndex` مثبّت على الاختلاف، لا استبدال ولا دمج ضمني — P0
- [x] T2 — قرار التنافي/الدمج عبر Resolver + تصحيح يدوي موثق — P0
- [x] T4 — التكامل مع التركيب والرسم والتصدير v8 — P0
- [x] T3 — واجهة الموضع متعدد الاختلافات — P0
- [x] T5 — الاختبارات — P0
- [x] التوثيق: `docs/EDITOR.md` + `docs/DATA.md` + تحديث هذا السجل

### قرارات أثناء التنفيذ (الأكثر أمانًا، تُوثَّق هنا)

- التجميع باتحاد المواضع (union-find) مع مفترق النطاق لكل موضع: اختلافان في
  الكلمة نفسها بمديين مختلفين تسلسل واحد، وقارئان مختلفان تسلسلان مستقلان.
- المحرر لا يقرر دمجًا أبدًا (P-07): سياسة «الفئات المتماثلة متنافية» حُذف
  استثناؤها المتصلّب، والتنافي = عكس `decideMerge` من المصفوفة والقواعد وحدها.
- القرار اليدوي رابط أولًا (`DIFFERENCE_TO_DIFFERENCE` بقرار `locusVerdict`)
  ثم يُترجَم: `RELATED` ← `MERGE` (يُطبَّقان معا)، `EXCLUSIVE` ← `REFERENCE`
  (لا يُضرَبان)؛ وقلب القرار تصحيح جديد لا تعديل شكلي.
- الإنشاء اليدوي والتصحيح خطوة تراجع واحدة عبر `extraEdits` في `addLink`،
  والزوج الواحد رابط واحد (إعادة القرار نفسه خاملة).
- اللصق يُجرِّد `occurrenceIndex` لتُعامَل النسخة ككيان جديد بفهرس تالٍ.
- الحذف لا يعيد الترقيم (P-03/DM-13): الفجوات في التسلسل مقبولة ومقصودة.
- العرض المرتبط بالقرار (`locusRelationStatuses`) إرشادي للواجهة فقط؛ القرار
  الفعلي في `resolveExclusiveGroups` قبل بناء التركيب.

### نتائج التحقق (2026-09-13)

- `npx tsc --noEmit`: سليم بلا أخطاء.
- `npx vitest run`: ‏47 ملفًا ناجحًا + 1 متجاوَز، 525 اختبارًا ناجحًا + 2 متجاوَزَين
  (الأساس قبل الحزمة: 504؛ الجديد: 21 في `tests/multi-difference-locus.test.ts`).
- `npm run build`: ناجح (Next.js، كل الصفحات).
