# تقدم حزم العمل

| الحزمة | المرحلة | الحالة | المتطلبات |
|---|---|---|---|
| 04 | PH3 | **تنفيذ جزئي — انتهت هذه الدفعة 2026-09-13؛ ليست مكتملة DoD** | FR-ED-04/05/06/07/13 · AC-04/05 · DM-03/10/15 |

بدأ التنفيذ في 2026-09-13 على فرع الجلسة `arena/01a09b21-tashjir`. لم يُستبدل المحرك ولا اختبارات التوافق.

## فحص افتراضات البدء

- نموذج v8 وDecision API موجودان، لكن المحرر يستخدم TashjeerDocument القديم وتحويلًا إلى v8 عند التصدير. أسطر Classic مشتقة من تركيبات الاختلافات؛ **ليست هناك ملكية مستقلة للاختلاف داخل Line محفوظ**.
- التحديد المفرد موحد؛ كان تحديد الأوجه المتعدد محليًا داخل بطاقة الاختلاف. نُقل تحديد الأوجه/الاختلافات المتعدد إلى مخزن المحرر.
- CommandLog موجود كوحدة، لكن المحرر يستخدم `past/future`. بُنيت العمليات الجديدة والقفز في التاريخ على سجل المستند نفسه، دون إنشاء سجل منافس أو الادعاء بإدماج مخازن القواعد الخارجية.
- السحب القديم جمع HTML5 للفأرة ومسار لمس منفصل: يبدأ بالفأرة فورًا، ولا يلتقط المؤشر، ويلتبس فهرس الإدراج برتبة 1-based؛ وكان pointercancel يستدعي الإفلات. أُزيل هذا المسار.
- القص القديم كان يحذف المصدر فورًا. أصبح القص تجهيزًا للحافظة، ولا يتغير المصدر إلا في معاملة لصق صالحة ومؤكدة.

## المنجز في هذه الدفعة

| المهمة | الأولوية | المنجز والدليل |
|---|---|---|
| **T1 / FR-ED-04 / AC-05** | P0 | سحب فعلي في لوحة ترتيب الأسطر بالفأرة واللمس/القلم عبر Pointer Events، ضغط ٣٥٠ms، التقاط المؤشر، مؤشر إدراج مضيء يشمل نهاية القائمة، Esc وpointercancel لا ينفّذان الإفلات. تأكيد يتضمن الرقم والجيران والرتبة النهائية وعدد المتأثرين؛ بدائل حقل الرتبة والأزرار وAlt+↑/↓. حفظ Line.order ومعرّفات الأسطر والأجزاء، وlineOrder للتوافق، وتتبع قبل/بعد. اختبار Chromium للسطر ١٠ → الرتبة ٢٠ ثم إلغاء/تأكيد/JSON/Undo/Redo، واختبارات منطق الفواصل والرتب والأجزاء والعلاقات. |
| **T4 / FR-ED-07 — الأوجه والاختلافات المحفوظة** | P0 جزئي | مخزن تحديد واحد، نقر/Ctrl/Shift/Ctrl+A بحسب القائمة المصفّاة، حذف جماعي مؤكد مع عدد العلاقات، أرشفة entities + links في deletedItems، وعدم فقد التحديد عند الإلغاء. خمسة أوجه تُحذف وتعود بتراجع واحد (اختبار مجال + متصفح). بقية مستويات T4 أدناه. |
| **T3 / FR-ED-06 — أساس الحافظة** | P0 جزئي | خدمة نقية واحدة لنسخ الأوجه/الاختلافات/الأجزاء، معرّفات جديدة للأوجه والأدلة، copiedFrom وطوابع جديدة واستقلال عميق وإعادة ربط ترتيب الأوجه. نقل الأوجه بين اختلافين داخل المستند يحافظ على IDs ويعيد توجيه العلاقات دفعة واحدة. لصق مؤكد دائمًا، ورفض المصادر المعدلة والوجهات غير الصالحة. العلاقات الخارجية محفوظة كمعلّقة في suspendedLinks وتظهر للمراجعة في الشريط. **هذا ليس AC-04 الخاص باختلافين بين سطرين مستقلين**. |
| **T2 / FR-ED-05 — مسار السحب** | P0 | مقبض ↳ يعمل بالضغط المطوّل، ملخص كمي، resolveLineMerge يجمع نتائج Decision API لكل فئات الأجزاء والعلاقة. المنع يظهر السبب والقواعد والأولوية وخيار تجاوز صريح ينشئ Correction. MERGE تحتفظ بمعرّفي الأصلين؛ mergeRecords تحفظ اللقطتين قبل الدمج، والسطر الباقي يحتفظ بمعرّفه. الفك مؤكد، ويرفض الحالة غير القابلة للفك مع السبب. اختبارات متصفح للدمج والفك، ومجال للسياسة والتجاوز والحفظ. |
| **T5 / FR-ED-13 / DM-15 — عمليات هذه الدفعة** | P0 | خطوة واحدة لكل نقل/دمج/فك/لصق/قص/حذف جماعي؛ سجل قابل للفتح والقفز المؤكد باستخدام التاريخ القائم. سلسلة نقل ← دمج ← لصق أوجه ← حذف خمسة أوجه ← ٤ Undo تعيد المستند حرفيًا، ثم ٤ Redo تعيد النهاية. مخازن القواعد/المواضع الخارجية لم تُضم بعد. |
| **DM-03/DM-10 والتخزين** | جزئي | إضافة Line snapshots ذات order صريح بصورة متوافقة، وحفظ الحقول الجديدة عند reload/import/export؛ نقل معرّفات أطراف العلاقات إلى IDs الفعلية في صورة v8 بدل بادئات العرض، وإلحاق العلاقة بكلا الاختلافين. تُصدّر أرشيفات الدمج والحذف والعلاقات المعلقة أيضًا. لا يُدّعى تحويل محرر التراكيب كله إلى نموذج ملكية Line مستقل. |

## المتبقي — وفق بروتوكول عدم الإكمال

### P0

1. **T4 / FR-ED-07**: تعميم التحديد والحذف الجماعي على **الأسطر والقواعد والمواضع المشتقة**، وعلى أوجه من عدة اختلافات في تحديد واحد. إلغاء Occurrence يجب أن يكون localOverride قابلًا للتراجع، مع خيار مستقل ومؤكد لحذف القاعدة الأم؛ لا يُحذف المصدر العام بالتخمين.
2. **T3 / FR-ED-06 / AC-04 / DM-10**: ملكية مستقلة لـDifference/Segment داخل Line، ثم نسخ/نقل «اختلاف ٢+٣» من Line 25 إلى Line آخر مع إبقاء باقي الوجهة كما هو. **غير منفذ**؛ ترفض الواجهة هذه الوجهة بدل تقديم لصق شكلي خاطئ.
3. **T3 / FR-ED-06**: نسخ **كيان Line كامل** بأجزائه واختلافاته وأوجهه ومعرّف سطر جديد، وحافظة القاعدة، ونقل الأجزاء/الاختلافات بين الأسطر، والنقل الذري بين مستندين. واجهة التوافق `copyLine` تنسخ اختلافات السطر فقط؛ لا تُعد قبولًا لنسخ السطر المستقل.
4. **T2 / FR-ED-05 — توافق المسارات القديمة**: تطبيق مسار التصحيح الصريح ذاته على كل تحويلات العلاقات المرجعية القديمة إلى دمج، وليس مقبض السحب الجديد فقط. أُبقيت واجهات التوافق وسلوك اختباراتها دون إعادة تصميم.
5. **T5 / FR-ED-13 / DM-15**: ضم عمليات القواعد وlocalOverride الخارجية إلى معاملة Undo واحدة مع المستند، ثم اختبار السلسلة المستهدفة نفسها بعد استكمال AC-04، ومراجعة بقية مداخل العمليات الفردية القديمة للتأكيد/الأرشفة.

### P1

- **FR-ED-06.4**: إجراء تفاعلي لإصلاح/إعادة ربط العلاقات المعلّقة؛ حاليًا تُحفظ وتُعرض بوضوح للمراجعة ولا تُفعّل.
- تقوية اختبارات السحب على أجهزة لمس فعلية ومتصفحات غير Chromium؛ اختبارات اللمس الحالية تولّد أحداث لمس حقيقية عبر CDP وليست مجرد mouse emulation.

### P2

- تحسين معاينة السطر الطويل أثناء السحب وتجربة التمرير التلقائي المستمر قرب الحواف.

## قرارات أمان البيانات

- ترتيب التنفيذ المتبع: T1 ← T4 المتاح في المستند ← T3 الآمن على النموذج القائم ← T2 ← T5. لم تُعالج فجوة ملكية الأسطر بترحيل جذري غير مختبر.
- الأوامر منخفضة المستوى تبقى متزامنة للتوافق؛ مداخل المستخدم الجديدة `request…` تطلب التأكيد وتتحقق من عدم تغير المستند/الوجهة أثناء الحوار.
- النسخ لا يحذف شيئًا. القص لا يحذف عند Ctrl+X. الوجهة غير المدعومة، والمصدر المعدّل بعد القص، والنقل بين مستندين، تُرفض مع رسالة والمصدر محفوظ.
- السحب موجود **في لوحة الترتيب داخل /editor**. /quran بلا تحرير. النقل أثناء تصفية الأسطر موقوف برسالة حتى لا تتلف رتب الأسطر المخفية.
- سجلات الحذف والدمج تحفظ الأصل لا نسخة مبتورة منه. الفك الذي يتعارض مع تعديلات لاحقة يرفض بأسباب؛ Undo يبقى المسار الآمن لاستعادة اللقطة السابقة.
- Enter داخل التأكيد يشغّل الزر المركّز (الافتراضي إلغاء)، مع حصر Tab وتعطيل اختصارات المستند أثناء الحوار.

## التحقق النهائي (2026-09-13)

أُعيد `npm ci` قبل التحقق النهائي؛ لم يتغيّر package-lock ولم تُضف تبعية دائمة.

| الأمر | النتيجة |
|---|---|
| `npm test` | **٥١٧ اختبارًا ناجحًا**، اختباران متخطيان سابقًا؛ ٤٧ ملفًا ناجحًا وملف متخطى |
| الاختبارات المحمية | manual-links + manual-correction + editor-manual-actions ناجحة ولم تُعدّل |
| `npm run typecheck` | ناجح |
| `npm run build` | ناجح، بلا تحذيرات جديدة في سجل البناء |
| `npm run test:e2e` | **٧ سيناريوهات Chromium ناجحة** بعد npm ci |
| ESLint للملفات التنفيذية المعدلة | ناجح |
| `git diff --check` | ناجح |

تتضمن الإضافات ١٣ اختبار مجال في `tests/editor-ph3.test.ts` و٧ اختبارات متصفح في `tests/e2e/editor-ph3.spec.ts`. تعذر تنزيل Chromium من CDN في البيئة؛ استُخدم executable مؤقت من حزمة متصفح دون حفظها في التبعيات أو Git، ثم أعيد npm ci. إعادة التشغيل العادية موثقة في docs/EDITOR.md.

**حصيلة القبول:** AC-05 ومسار السحب منفذان ومختبران؛ AC-04 ونسخ Line الكامل وكل مستويات T4 لم تُجتز. لا تُغلق الحزمة 04 قبل استكمال بنود P0 أعلاه.
