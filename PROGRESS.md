# سجل التقدم — Tashjir

> البروتوكول الملزم (الدليل 00 §3): يُحدَّث هذا الملف في بداية كل جلسة ونهايتها.
> الحالة المرجعية: `main` عند PR #71 (الالتزام `24010c6`).
> ملاحظة: ملفات الحزم 01→12 تُعطى للوكيل تباعًا من صاحب المشروع؛ ما لم يُستلم
> ملف حزمة بعد، تُسجَّل الجلسات التحضيرية تحت «خط الأساس» أدناه.

| الحزمة | الحالة | المنجز (بالمعرفات) | المتبقي (بالمعرفات + أولوية) | ملاحظات |
|--------|--------|--------------------|------------------------------|---------|
| خط الأساس (إصلاح ضرر الدمج) | مكتملة | إصلاح 5 ملفات + تحقق أخضر كامل: 604 اختبارًا ناجحًا، `typecheck` نظيف، `eslint` نظيف، `build` ناجح (17 مسارًا)، فحص دخاني لـ `/editor` `/studio` `/quran` `/tracking` | — | ضرر دمج PR #71 (سطور مأكولة في `editor-store` و3 مكوّنات) |
| 01 → 12 | لم تبدأ | — | بانتظار استلام ملفات الحزم من صاحب المشروع (P0) | `docs/SRS_STATUS.md` يشير إلى تنفيذ واسع سابق؛ يُعتمد التحقق مقابل معايير قبول كل حزمة عند استلامها |

## قرارات تفسيرية

- [2026-09-14] خط الأساس: أُعيد بناء ما أكله الدمج في `src/stores/editor-store.ts`
  (`deleteAlternative`، `deleteVariantsBulk`، `requestDeleteItems`، دوال `selectWord`/
  `selectVariant`/`selectAlternative`/`selectSegment`/`selectLine`، سطرا
  `document: restored` في `undo`/`redo`) — السلوك مثبّت بالاختبارات القائمة
  (`editor-bulk-actions`، `editor-ph3`، `selection-store`) لا بالاجتهاد.
- [2026-09-14] خط الأساس: `restoreHistoryEntry` تعيد اللقطة حرفيًا بلا إعادة
  توليد للخطوط؛ إعادة التوليد كانت تكسر تطابق التراجع/الإعادة بايتًا الذي
  تحرسه اختبارات `editor-ph3` (اللقطات مخزّنة مولّدة أصلًا من `mutate`).
- [2026-09-14] خط الأساس: `requestDeleteItems` يؤكد بحوار كمي
  («حذف ٥ أوجه؟» + عدد الروابط المؤرشفة) ثم يفوّض إلى `deleteAlternativesBulk`
  / `deleteVariantsBulk` — سلوك واحد في مكان واحد، ويفحص قِدَم المستند بعد التأكيد.
- [2026-09-14] خط الأساس: مسار التعميم المأكول في `SmartCreateWizard` أُعيد بناؤه
  على `saveGlobalRuleBatch` (دفعة واحدة برتب متجاورة ومعرّف دفعة مشترك DM-08)
  داخل `transactExternal` (خطوة تراجع موحدة + سطر تتبع)؛ عنوان القاعدة
  «نص التحديد — اسم الفئة» ودرجتها من أول وجه مُدخل، ورسالة الإتمام تذكر عدّ
  المواضع المطابقة عند اكتمال الـ Dry-run.
- [2026-09-14] خط الأساس: قائمة `VariantsPanel` أُعيد تغليفها بـ `ScrollableList`
  (رأس بحث فوري + `emptyState` + `activeIndex` من التحديد الموحد + `renderWindow`
  للتنافذ) **بإغلاق ذاتي `/>`** — محلل TSX يرفض العنصر فارغ الأطفال الذي آخر
  خاصية فيه سهمٌ يُرجع JSX مقوّسًا (تحقق تجريبي بملف معزول).
- [2026-09-14] خط الأساس: `onSelect` الصف (Shift للمدى، Ctrl للإضافة على
  الاختلافات الظاهرة FR-ED-07) أُعيد بناؤه عبر `selectRange` على
  `multiSelection`؛ ووُظّف `isChecked` المهمل لإبراز الصفوف المحددة (يغذّي
  نسخ Ctrl+C للاختلافات المتعددة عبر `copySelection`).
- [2026-09-14] خط الأساس: `confirmLegacy` في `LinksList` (تحويل/حذف علاقة، حذف
  جزء) حوار كمي عام قابل للتراجع بدل `window.confirm`؛ وقائمة العلاقات غُلّفت
  بـ `ScrollableList` (نمط الأطفال كـ `RulesIndexDialog`) إعمالًا للاستيراد
  الموجود وFR-ED-01.
- [2026-09-14] خط الأساس: `HistoryControls` يقرأ `entry.document` من لقطات
  السجل الموحدة بدل افتراض أنها مستندات مباشرة.

## متابعة مقترحة لجلسات الحزم (غير حاجبة)

- شريط إجراءات جماعية للاختلافات المحددة (حذف N اختلافات عبر `requestDeleteItems`
  من `multiSelection` نوع `DIFFERENCE`) — لا مشغّل واجهي له بعد سوى نسخ Ctrl+C.
- لا منتج لخاصية `data-list-index` (يستعلم عنها `ScrollableList` للتمركز الدقيق) —
  التمركز يعمل بالتقدير الاحتياطي.
- `deleteAlternativesBulk` يسجّل `changes` قبل/بعد مقروءين كلاهما من حالة ما بعد
  الحذف (تجميلي في بيانات التتبع).
- القبول اليدوي AC-01..AC-06 من الواجهة ما زال مطلوبًا (الحزمة 12) — الفحص الآلي
  لا يغني عنه.
