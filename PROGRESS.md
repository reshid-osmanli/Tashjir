# سجل التقدم — Tashjir

> البروتوكول الملزم (الدليل 00 §3): يُحدَّث هذا الملف في بداية كل جلسة ونهايتها.
> الحالة المرجعية: `main` عند PR #71 (الالتزام `24010c6`).
> ملاحظة: ملفات الحزم 01→12 تُعطى للوكيل تباعًا من صاحب المشروع؛ ما لم يُستلم
> ملف حزمة بعد، تُسجَّل الجلسات التحضيرية تحت «خط الأساس» أدناه.

| الحزمة | الحالة | المنجز (بالمعرفات) | المتبقي (بالمعرفات + أولوية) | ملاحظات |
|--------|--------|--------------------|------------------------------|---------|
| خط الأساس (إصلاح ضرر الدمج) | مكتملة | إصلاح 5 ملفات + تحقق أخضر كامل: 604 اختبارًا ناجحًا، `typecheck` نظيف، `eslint` نظيف، `build` ناجح (17 مسارًا)، فحص دخاني لـ `/editor` `/studio` `/quran` `/tracking` | — | ضرر دمج PR #71 (سطور مأكولة في `editor-store` و3 مكوّنات) |
| 01 — الأساس: نموذج البيانات v8 + الترحيل + التراجع + حلّ القرار | مكتملة | DM-01 Difference كيان كامل، DM-02 Variant مستقل، DM-03 Relation بمعرفات فقط، DM-04 رتبة صريحة Line.order/Variant.rank/displayOrder، DM-05 Source & Correction ثلاثية، DM-06 سياق وقف/وصل، DM-07 WaqfMark، DM-08 GlobalRule+RuleOccurrence+localOverride، DM-09 تعدد اختلافات لنفس القارئ+موضع مع occurrenceIndex، DM-10 Line، DM-11 RenderRange، DM-12 createBatchId، DM-13 حتمية تصدير Git-friendly، DM-14 engineConfig كتلة مستقلة، DM-15 سجل تراجع موحد CommandLog، DM-17 تمثيل السطر، DM-18 ترحيل v7→v8، FR-EN-01 فصل منطق عن سياسة، FR-EN-02 Decision Resolver مركزي، FR-EN-03 Decision API موحدة، FR-EN-04 ثلاثية محرك/محرر/نهائي، P-01→P-14 مبادئ ملزمة، NFR-04 حفظ آمن مع نسخ احتياطي، NFR-05 ترحيل آمن، NFR-06 حتمية، 624 اختبارًا ناجحًا + typecheck + build | — | استيراد jeson_exemp/tashjeer-2-4.json و tashjeer-2-45.json (v7) ← ترحيل تلقائي بنسخة احتياطية ← تصدير v8 ← إعادة استيراد بلا فقد، تصدير مرتين byte-stable، ناتج المحرك متطابق قبل/بعد، كل قرار دمج/ترتيب/تنافي عبر Decision API |
| 02 — Engine Studio Core (FR-ES-01..06, 10, 14, 16) | مكتملة | FR-ES-01 نظام الأولوية (أرقام صريحة + مجموعات + سحب بإزاحة دقيقة + شريط بصري + إشعار تصادم)، FR-ES-02/03 منشئ القواعد وشروطها/إجراءاتها بلا كود + اختبار موضع بلا حفظ، FR-ES-04 خط أنابيب القرار + بوابة تحذير/قياس أثر قبل حفظ الترتيب (`executionOrderImpact`)، FR-ES-05 مصفوفة الدمج والعلاقات (تعديل صف ← قرار تالٍ + ذكره في الأثر)، FR-ES-06 حل التعارض/الخصوصية/التجاوز/التوريث (عام + تجاوز محلي موثق)، FR-ES-10 Why؟/Trace مكتمل (INPUT→FINAL مع سبب لكل خطوة)، FR-ES-14 تصدير/استيراد حتمي + diff دقيق (سطر واحد) + رفض العبث، FR-ES-16 الفئات الـ14 + قوالب الوقف/الوصل؛ T1 إعدادات المحرك من /admin → /studio?section=settings، T10 اختبارات حتمية (21 اختبارًا في `tests/engine-studio-package02.test.ts`)، 645 اختبارًا أخضر + typecheck + build | — | تنفيذ سابق واسع وُجد في `main`؛ هذه الجلسة أتمّت الفجوات الناقصة مقابل معايير القبول (راجع قرارات تفسيرية 2026-09-14) |
| 03 — التحديد الموحد والقوائم الاحترافية (FR-ED-01 · FR-ED-02 · AC-06 · NFR-01) | مكتملة | FR-ED-02 بالكامل: مخزن تحديد موحّد (selection + selectionWrite + selectionFocus برقم متزايد + lastSelection رمادي عند الحذف)، أنواع التحديد العشرة (كلمة/حرف/موضع/سطر/جزء/اختلاف/وجه/قاعدة/وجه مركب/علامة وقف)، سلسلة السياق Breadcrumb بشريط علوي دائم، انتقال اللوحة للعنصر وتمييزه (Scroll Into View + pan تمركز + نبضة توهج + بطاقة معلومات) ≤ 300ms (AC-06)، لوحة تفاصيل العنصر (معرّف/رتبة/آية/صفحة/فئة/قواعد/اختلافات موضع/علاقات/أجزاء/مصدر/حالة)، قائمة سياق وأوامر مشتركة (نسخ/قص/لصق/حذف/ترتيب/ربط/لماذا/نسخ معرف)، لغة إبراز موحدة (زمردي للتحديد/كهرماني للتوجيه)، رابط عميق من /quran و/tracking و/studio للمحرر؛ FR-ED-01: ScrollableList معممة على قائمة الاختلافات (بحث فوري + تنافذ + تمركز مقيس data-list-index) والعلاقات وترتيب الأسطر وسجل تتبع القاعدة والفهرس وقواعد الاستوديو (رأس ثابت + شريط مرئي + زرا صعود/نزول بضغط مستمر + زر أعلى القائمة + تمرير عجلة/لمس/أسهم + تحميل تدريجي/تنافذ 2000+)؛ NFR-01 مختبرة (تصفية 1000 + 120 خطوة تمرير على 2000)؛ T3: 654 اختبارًا ناجحًا + typecheck + build | — | وُجد تنفيذ واسع سابق في `main` (متوافق مع بروتوكول «افحص ثم ابنِ»): الجلسة دققت كل معيار قبول وأكملت الفجوات فقط — تمركز مقيس بإعادة محاولة محدودة، تعميم القائمة الاحترافية على ترتيب الأسطر وسجل التتبع (مع حفظ سحب الإزاحة الدقيقة على الحاوية الجديدة)، حارس إعادة تسمية القارئ، استكمال قائمة الاختصارات، اختبار عقد رسم القائمة |
| 04 → 12 | لم تبدأ | — | بانتظار استلام ملفات الحزم من صاحب المشروع (P0) | `docs/SRS_STATUS.md` يشير إلى تنفيذ واسع سابق؛ يُعتمد التحقق مقابل معايير قبول كل حزمة عند استلامها |

## قرارات تفسيرية

- [2026-09-14] خط الأساس: أُعيد بناء ما أكله الدمج في `src/stores/editor-store.ts`
  (`deleteAlternative`، `deleteVariantsBulk`، `requestDeleteItems`، دوال `selectWord`/
  `selectVariant`/`selectAlternative`/`selectSegment`/`selectLine`، سطرا
  `document: restored` في `undo`/`redo`) — السلوك مثبّت بالاختبارات القائمة
  (`editor-bulk-actions`، `editor-ph3`، `selection-store`) لا بالاجتهاد.
- [2026-09-14] خط الأساس: `restoreHistoryEntry` تعيد اللقطة حرفيًا بلا إعادة
  توليد للخطوط؛ إعادة التوليد كانت تكسر تطابق التراجع/الإعادة بايتًا الذي
  تحرسه اختبارات `editor-ph3` (اللقطات مخزّنة مولّدة أصلًا من `mutate`).
- [2026-09-14] خط الأساس: `requestDeleteItems` يؤكد بحوار كمي
  («حذف ٥ أوجه؟» + عدد الروابط المؤرشفة) ثم يفوّض إلى `deleteAlternativesBulk`
  / `deleteVariantsBulk` — سلوك واحد في مكان واحد، ويفحص قِدَم المستند بعد التأكيد.
- [2026-09-14] خط الأساس: مسار التعميم المأكول في `SmartCreateWizard` أُعيد بناؤه
  على `saveGlobalRuleBatch` (دفعة واحدة برتب متجاورة ومعرّف دفعة مشترك DM-08)
  داخل `transactExternal` (خطوة تراجع موحدة + سطر تتبع)؛ عنوان القاعدة
  «نص التحديد — اسم الفئة» ودرجتها من أول وجه مُدخل، ورسالة الإتمام تذكر عدّ
  المواضع المطابقة عند اكتمال الـ Dry-run.
- [2026-09-14] خط الأساس: قائمة `VariantsPanel` أُعيد تغليفها بـ `ScrollableList`
  (رأس بحث فوري + `emptyState` + `activeIndex` من التحديد الموحد + `renderWindow`
  للتنافذ) **بإغلاق ذاتي `/>`** — محلل TSX يرفض العنصر فارغ الأطفال الذي آخر
  خاصية فيه سهمٌ يُرجع JSX مقوّسًا (تحقق تجريبي بملف معزول).
- [2026-09-14] خط الأساس: `onSelect` الصف (Shift للمدى، Ctrl للإضافة على
  الاختلافات الظاهرة FR-ED-07) أُعيد بناؤه عبر `selectRange` على
  `multiSelection`؛ ووُظّف `isChecked` المهمل لإبراز الصفوف المحددة (يغذّي
  نسخ Ctrl+C للاختلافات المتعددة عبر `copySelection`).
- [2026-09-14] خط الأساس: `confirmLegacy` في `LinksList` (تحويل/حذف علاقة، حذف
  جزء) حوار كمي عام قابل للتراجع بدل `window.confirm`؛ وقائمة العلاقات غُلّفت
  بـ `ScrollableList` (نمط الأطفال كـ `RulesIndexDialog`) إعمالًا للاستيراد
  الموجود وFR-ED-01.
- [2026-09-14] خط الأساس: `HistoryControls` يقرأ `entry.document` من لقطات
  السجل الموحدة بدل افتراض أنها مستندات مباشرة.
- [2026-09-14] الحزمة 01 — T1 نموذج البيانات v8: النموذج كان موجودًا جزئيًا في `src/lib/tashjeer/model/v8.ts` لكنه لم يغطِّ كل حقول المواصفة صراحة؛ أُضيف `safe-save.ts` لتحقيق NFR-04 (حفظ آمن + نسخ احتياطية قبل العمليات الواسعة) وDM-13 (تصدير حتمي Git-friendly). الأنواع DM-01→DM-12 وDM-17 موجودة كاملة مع أمثلة JSON في `docs/SCHEMA.md` ومخطط كامل في `docs/DATA.md`.
- [2026-09-14] الحزمة 01 — T2 Decision Resolver: السياسات الافتراضية في `decision/policy.ts` تطابق سلوك المحرك الحالي حرفيًا (لا تغيير ناتج). المحركات `ordering`, `combination-engine`, `classic-tashjeer` وُجهت عبر Decision API:
  - `ordering.ts`: كان يستخدم مقارنة مباشرة لـ `orderRank` و`alternativeOrder`؛ الآن يستدعي `resolveOrder` من `decision/api.ts` للرتب الصريحة (DM-04) قبل تطبيق خطة القراءة وقوة الوجه — الناتج متطابق بفضل نفس منطق الرتبة.
  - `combination-engine.ts`: كان يحسب التنافي محليًا؛ الآن يمر عبر `resolveExclusiveGroups` في `decision/editor-bridge.ts` الذي يستدعي `resolveMerge` و`resolveRelationExclusion` من Decision API.
  - `classic-tashjeer.ts`: يمرر `engineConfig` إلى `combination-engine` ويستخدم `chipsForUnits` التي تقرأ الرموز من الكتالوج مع `displayOrder` الصريح.
  - قائمة الثوابت القديمة المحوّلة: `CATEGORY_PRIORITY` في `branch-engine.ts` أصبحت تُقرأ عبر `mergeMatrix` و`conflictPolicy` في `EngineConfig`؛ `DEFAULT_MERGE_MATRIX` في `policy.ts` هي المصدر الوحيد؛ `compareVariantsForReading` و`compareAlternatives` لم تعد تحتوي ثوابت ترتيب داخلية بل تستدعي `resolveOrder`.
- [2026-09-14] الحزمة 01 — T3 ثلاثية المحرك/المحرر/النهائي: `Correction` موجودة وتُنشأ في `migrate-v7-v8.ts` (من `engineSnapshot`) وفي `merge-operations.ts` عند تجاوز يدوي لسياسة الدمج. `engineSnapshot` في `Difference` يحفظ A دائمًا، و`source/modified_by` يميز المحرك من المحرر (DM-05، P-06). بنية البيانات والتصنيف هنا؛ واجهات العرض في حزم لاحقة.
- [2026-09-14] الحزمة 01 — T4 الترحيل v7→v8: `migrateDocumentToV8` دالة نقية لا تعدل الأصل، تحفظ كل معرف (P-03)، وتولد `occurrenceIndex` لتعدد الاختلافات (DM-09)، وتحوّل `NO_WASL`→`FORBIDDEN_WASL`. `importDocuments` ينشئ نسخة احتياطية بمفتاح `tashjeer:backup:` قبل الترحيل (NFR-04) ويبلغ عنها في `ImportResult.migrated[]`. اختبار الانحدار على `jeson_exemp/tashjeer-2-4.json` و`tashjeer-2-45.json` في `tests/jeson-exemp-v7-v8.test.ts`.
- [2026-09-14] الحزمة 01 — T5 سجل التراجع الموحد: `CommandLog` يغطي كل العمليات المطلوبة (نقل، دمج، فصل، حذف فردي/جماعي، لصق، تعميم، إنشاء مجموعة، إعادة ترتيب، تعديل رتبة، وقف، تجاوز محلي) مع `transaction` للدفعات و`jumpTo`. `editor-store` يلتقط لقطة ثلاثية (مستند + استثناءات + قواعد) عبر `captureHistoryEntry`/`restoreHistoryEntry` فلا يبقى أثر معلق (FR-ED-10).
- [2026-09-14] الحزمة 01 — T6 حتمية التصدير: `toCanonicalConfig` يرتب القواعد بمعرفها ومصفوفة الدمج بمفاتيحها بلا طوابع متقلبة (DM-13). `toStableV8` يولد معرفات حتمية للتصحيحات ونطاقات العرض من `ayahKey` و`targetId`. `buildExportBundle` يبني `v8[]` و`engineConfig` و`displayOrder[]` بترتيب حتمي. اختبار byte-stable في `import-migration-v8.test.ts` و`jeson-exemp-v7-v8.test.ts`.
- [2026-09-14] الحزمة 01 — T7 الحفظ الآمن (P1): `safe-save.ts` يطبق كتابة ذرية `atomicWrite` (setItem مرة واحدة)، و`backupBeforeWideOp` قبل العمليات الواسعة، و`AutoSaveManager` للحفظ الدوري + عند العمليات الخطرة. `document-store.ts` يطبق `persistMigrationBackup` قبل كل ترحيل ويحفظ `displayOrder` مع التصدير. التأكيد الكمي قبل كل حذف/دمج عبر `confirmAction` (P-13).
- [2026-09-14] الحزمة 01 — خارج النطاق: لم تُضف أي واجهة استخدام جديدة (المعالجات، السحب، التحديد الموحد، Engine Studio UI، الوقف/الوصل UI) — كلها حزم لاحقة. السياسات الافتراضية تطابق السلوك الحالي حرفيًا؛ لا تغيير قواعد فعلية.

- [2026-09-14] الحزمة 02 — النطاق: وُجد في `main` تنفيذ واسع لـ `/studio` من جلسة
  سابقة (الصفحة + 13 مكوّنًا + المخازن + التوثيق). اعتمدتُ «افحص ثم ابنِ»:
  تحقّقت من كل معيار قبول مقابل الكود الفعلي، وأكملت الفجوات الناقصة فقط —
  لا إعادة كتابة لما يعمل.
- [2026-09-14] الحزمة 02 — إزاحة تصادم الأولويات: `setRulePriority` كانت تزيح **كل**
  قاعدة أعلى من الرقم الجديد (+1) — إزاحة شاملة كسرت حتمية «سحب B فوق A»
  (أرقام 21/10 بدل 20/10) وكسرت عقد DM-13 «تغيير أولوية = سطر واحد». بُدّلت
  بـ `applyPriorityShift`: تُزاح المتصادمات بالرقم **سلسلةً** +1 فقط، وتُعاد
  أسماءها وأرقامها لإشعار المستخدم (FR-ES-01 «إزاحة خطوة 1 مع إشعار، قابلة
  للتراجع» — التراجع عبر سجل الإصدارات). اختبار «إسناد أولوية مشغولة يزيح
  القاعدة الأخرى» (الحزمة 01) ما زال أخضر بالحرف.
- [2026-09-14] الحزمة 02 — عدّاد المراجع خارج التصدير: `version` لكل قاعدة (يُزاد مع
  كل تعديل) أصبح خارج الصيغة الكنسية المصدّرة — وإلا صار تعديل أولوية واحد
  سطرين في الـ diff (الأولوية + الإصدار)، فيخالف AC-5 «سطرًا واحدًا ذا معنى».
  الملف المصدَّر وثيقة سياسة: `schema-version` يوثّق الصيغة، وعدّاد المراجع
  يُعاد من 1 عند الاستيراد (يُعاد توليد نسبه محليًا). لا اختبار كان يعتمد على
  حقل `version` في التصدير.
- [2026-09-14] الحزمة 02 — بوابة ترتيب المراحل (FR-ES-04): إعادة ترتيب خط الأنابيب
  كانت فورية بلا تحذير. الآن: تحذير واضح + قياس أثر مبدئي قبل الحفظ
  (`executionOrderImpact` للمراحل/القواعد المتأثرة + `runProfileTests` على
  الترتيب الجديد) عبر `confirmAction` الكمي؛ الإلغاء يعيد الترتيب القديم
  (المكوّن مُتحكَّم بـ `config.executionOrder`).
- [2026-09-14] الحزمة 02 — Why للتجاوز الوحيد: `resolveConflictPolicy` كانت تعيد
  «مرشّح واحد» حتى لو كان المرشّح تجاوزًا صريحًا موثقًا؛ صارت تميّز
  «Explicit Override موثق (الاسم)» فيظهر السبب الصحيح في سلسلة القرار (T6:
  عام ← تجاوز محلي).
- [2026-09-14] الحزمة 02 — توثيق: `docs/ENGINE_STUDIO.md` (الشريط البصري، إشعار
  التصادم، بوابة المراحل، عدّاد المراجع خارج التصدير) و`docs/API.md`
  (عمليات ملف المحرك النقيّة + `executionStageOf`/`executionOrderImpact`).
  صفحة `/studio` موجودة أصلًا في جدول صفحات README والتنقل باسم «استوديو المحرك».

## متابعة مقترحة لجلسات الحزم (غير حاجبة)

- شريط إجراءات جماعية للاختلافات المحددة (حذف N اختلافات عبر `requestDeleteItems`
  من `multiSelection` نوع `DIFFERENCE`) — لا مشغّل واجهي له بعد سوى نسخ Ctrl+C.
- `deleteAlternativesBulk` يسجّل `changes` قبل/بعد مقروءين كلاهما من حالة ما بعد
  الحذف (تجميلي في بيانات التتبع).
- القبول اليدوي AC-01..AC-06 من الواجهة ما زال مطلوبًا (الحزمة 12) — الفحص الآلي
  لا يغني عنه.

## قرارات تفسيرية إضافية (الحزمة 03 — 2026-09-14)

- [2026-09-14] الحزمة 03 — «افحص ثم ابنِ»: وُجد في `main` تنفيذ الحزمة كاملًا
  تقريبًا من جلسة سابقة (مخزن التحديد الموحّد في `editor-store` +
  `lib/editor/selection-store.ts` + `ScrollableList` + `useWindowedList` +
  `windowed-list-core` + لوحات Breadcrumb/Details/FocusCard/ContextMenu +
  اختبارات `selection-*` و`windowed-list-performance` كلها خضراء). الجلسة
  دققت كل بند من T1/T2/T3 ومعايير القبول مقابل الكود الفعلي وأكملت الفجوات
  المحدقة فقط؛ لم يُحذف أو يُعاد كتابة أي شيء يعمل.
- [2026-09-14] الحزمة 03 — تمركز Scroll Into View الدقيق: `data-list-index`
  لم يكن يُنتج في أي قائمة (ملاحظة المتابعة السابقة)، فكان تمركز «العنصر ٨٤٧
  في منتصف الرؤية» يعمل بالتقدير الاحتياطي فقط. الآن: صفوف قائمة الاختلافات
  تحمل فهرسها وتقيس ارتفاعها عبر `range.measure` (يغذي تنافذ القائمة أيضًا)،
  و`scrollToIndex` تمركز بالصف المقيس؛ وإن كان الصف خارج النافذة المرسومة
  تقفز بالتقدير فورًا ثم تعيد التمركز الدقيق على الصف المرسوم بعد التنافذ —
  محاولات محدودة (12 إطارًا ≈ 200ms) داخل ميزانية AC-06، بلا دورة لا نهائية.
  حاوية تمرير القائمة تحمل `data-scroll-viewport` ليكتشفها منطق خارجي.
- [2026-09-14] الحزمة 03 — تعميم القائمة الاحترافية على «ترتيب الأسطر»
  (`LineOrderEditor`): كانت قائمة `max-h-80 overflow-y-auto` عادية بلا رأس
  ثابت ولا أزرار صعود/نزول ولا انتقال للسطر النشط. لُفّت بـ `ScrollableList`
  مع `activeIndex` من التحديد الموحّد (سطر محدد من لوحة أخرى يُمرَّر إليه
  ويُميَّز) — مع حفظ سحب الإزاحة الدقيقة كما هو: حدود السحب والتمرير التلقائي
  أثناء السحب تُحسب الآن على حاوية التمرير (`closest('[data-scroll-viewport]')`)
  لا على القائمة نفسها لأن امتدادها أطول من الرؤية، و`data-list-index` على
  صفوف الترتيب يمكّن التمركز الدقيق.
- [2026-09-14] الحزمة 03 — سجل تتبّع مواضع القاعدة (`RuleOccurrenceReview`
  تبويب «سجل التغييرات»): كان يرث تمرير الحوار العام بلا ميزات القائمة
  الاحترافية. لُفّ تبويب السجل بـ `ScrollableList` (رأس ثابت بعدّ المدخلات
  + زرا صعود/نزول + زر أعلى القائمة)؛ تبويب «استعراض المواضع» بقي على تمريره
  العام عمدًا لأنه بطاقة واحدة كبيرة بلا قائمة صفوف.
- [2026-09-14] الحزمة 03 — حارس «إعادة تسمية قارئ لا تغيّر ترتيبه»: أُضيف
  إلى `tests/catalog-order.test.ts` ما يثبت أن مفتاح ترتيب الأئمة والرواة
  (طيبة أولًا ثم `order`) والطرق هو الرقم الصريح والاسم كاسر تعادل فقط —
  إعادة تسمية بما يقلب الترتيب الأبجدي لا تحرك أي عنصر، عبر دوال الترتيب
  الموحدة وفي `normalizeTransmissionCatalog` كذلك.
- [2026-09-14] الحزمة 03 — عقد رسم القائمة محروس نصيًا: `tests/scrollable-list-render.test.ts`
  يولّد `ScrollableList` على الخادم (نفس أسلوب `figure-render`) ويتحقق من
  حاوية التمرير (`data-scroll-viewport` + `role=region` + `tabindex` +
  `overflow-y-scroll`) والرأس الثابت والتنافذ (المرئي يُرسم والبعيد لا +
  حشوات `aria-hidden`) وحالة الفراغ. (ملاحظة عامة: `useSyncExternalStore`
  في zustand v5 يقرأ لقطة الخادم من حالة الإنشاء لا من `setState`، فاختبار
  رسم نصي لمكوّن يقرأ المخزن مباشرة غير ممكن بلا تسويف — لذلك اختبارات
  المخزن منطقية عبر `getState`، وعقد الرسم النصي يقتصر على مكوّنات نقية
  تستقبل خصائصها.)
- [2026-09-14] الحزمة 03 — استكمال قائمة الاختصارات الموثقة: `N` (المعالج
  الذكي) وCtrl+C/X/V (أوامر العنصر النشط عبر التحديد الموحّد) كانت منفذة في
  `useKeyboardShortcuts` لكنها غائبة من `SHORTCUT_HINTS` (نافذة المساعدة)
  وجدول `docs/EDITOR.md` §7 — أضيفت في الموضعين فصار مصدر التوثيق مطابقًا
  للتنفيذ (لا اختصارات جديدة أُضيفت لهذه الحزمة).

