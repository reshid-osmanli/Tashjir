# سجل تقدم الحزم — Tashjir Work Packages Progress

> تحديث: 2026-09-13 — الحزمة 06 مكتملة على فرع `arena/01a09b22-tashjir`.

| الحزمة | المرحلة | الحالة | المتطلبات | ملاحظات |
|---|---|---|---|---|
| 01 — النموذج الموحد v8 | PH0 | ✅ موجود في `main` | DM-01…DM-14 | `model/v8.ts` + ترحيل + `createBatchId` — تحقق منها قبل البدء |
| 02 — سياسات Resolver | PH1 | ✅ موجود في `main` | FR-ES | `decision/resolver.ts` + مصفوفة الدمج — يُقرأ اقتراحها في الخطوة 5 |
| 03 — التحديد الموحد | PH2 | ✅ موجود في `main` | FR-ED-02 | `selection-context.ts` + `selection` واحد في المخزن |
| 04 — الحافظة والحذف الجماعي | PH3 | ✅ موجود في `main` | FR-ED-06/07 | clipboard متعدد + حذف دفعي + تأكيد كمي |
| 05 — تعدد الاختلافات | PH4 | ✅ موجود في `main` | FR-ED + DM-09 | `occurrenceIndex` + استقلال الموضع الواحد |
| **06 — الإنشاء الذكي الموحد** | **PH5** | ✅ **مكتملة** | **FR-ED-08 · FR-ED-09 (+ NFR-02)** | المعالج ٧ خطوات + إسناد دفعي + تعميم ذري + توحيد المداخل |
| 07 — التحرير المحلي بعد التعميم | PH6 | ⬜ لم تبدأ | localOverride | خارج نطاق 06 |
| 08 — منطق الوقف/الوصل التنفيذي | PH7 | ⬜ لم تبدأ | — | حقل `context` يُضبط ويُحفظ في 06؛ سلوكه التنفيذي هنا |
| 09 — القواعد في قائمة الاختلافات | PH8 | ⬜ لم تبدأ | FR-ED-15 | — |

## الحزمة 06 — ما أُنجز (2026-09-13)

النواة (`smart-create.ts` + `SmartCreateWizard.tsx`) كانت موجودة جزئيًا في
`main`؛ هذه الحزمة أكملتها وأغلقت كل فجوات المواصفة:

- **الخطوة 1 (التحديد)**: مدى كلمة→كلمة بآلة نقر نقيّة (`nextRangeClick`)
  مع شارة «المدى مثبّت قبل الإنشاء»؛ Ctrl+نقر لأهداف متفرقة
  (`toggleWordTarget`)؛ تحديد حروف داخل المعالج (المتباعد أهداف تلقائية)؛
  استقبال تعليم المحرر كاملًا كأهداف؛ وضعا «تكرار البنية لكل هدف» و«موضع
  مركّب واحد».
- **الخطوة 2–3 (الأنواع والأوجه)**: checkboxes متعددة؛ لكل نوع نص الوجه
  المقروء ودرجة قوته من سلّم الدرجات (`strengthDegreeId` تُحفظ على الوجه).
- **الخطوة 4**: محدد النطاق القائم كما هو.
- **الخطوة 5 (العلاقات)**: اقتراح Resolver (`decideMutualExclusion`) معروض
  مع سببه لكل زوج، والافتراض المخصص يتبعه — لا علاقات خفية.
- **الخطوة 6 (التعميم)**: نمط حتمي من الحروف **أو من الكلمات كاملة**
  (`buildCharacterPatternForWords`)؛ سياسة ضبط ونطاق بحث وتوسيع الحروف
  إلى المجموعات؛ **Dry-run غير حاجب** سورةً سورة (`mushafSurahIndex` +
  `countGlobalRuleMatchesInSurah`) مع مؤشر تقدم وإلغاء وعدد المتطابقة
  والمتخطاة؛ حفظ ذري دفعي (`saveGlobalRulesBatch`: تحقق ثم كتابة واحدة)؛
  زر «المنشئ الكامل» للأنماط الصرفية/النحوية المتقدمة.
- **الخطوة 7**: سياق للمجموعة + تجاوز مستقل لكل نوع (`contextByType`)؛
  مراجعة عددية؛ تنفيذ ذري بدفعة Undo واحدة وتسجيل في التتبع.
- **القوالب والتفضيلات**: حفظ الإعداد الحالي قالبًا مسمى، قوالب مستخدم
  دائمة، وضع متقدم يتخطى الخطوات الافتراضية (1→7 مباشرة) —
  `wizard-templates.ts`.
- **T2 (FR-ED-09)**: `buildSmartCreateMultiTargetBatch` — نسخة كاملة
  (أنواع+أوجه+علاقات داخل الهدف) لكل كلمة بعناوينها، بمعرّف دفعة واحد؛
  Ctrl+نقر على اللوحة يعلّم دون تبديل الأداة.
- **T3 (توحيد المداخل)**: حُذفت قوائم الإنشاء المكررة من لوحة الاختلافات
  (الدفعي المضمّن/المفرد/لكل موضع) وحلت محلها: المعالج (زر + `N`) وشرائح
  ⚡ الإنشاء السريع (مدمجة + قوالب المستخدم) عبر نواة المعالج نفسها.
  منشئ القواعد الكامل باقٍ كقدرة متقدمة يُفتح من الخطوة ٦ مزروعًا —
  الباب توحّد والقدرة لم تُحذف.
- **T4**: 25 اختبارًا جديدًا (529 إجمالي، كلها خضراء) + `typecheck` +
  `build` ناجحان.

## معايير القبول — التغطية

| # | المعيار | الحالة |
|---|---|---|
| 1 | مد+تحقيق+صلة+فرش في عملية واحدة، كيانات مستقلة | ✅ قالب «مد + تحقيق + صلة» + اختبار الاستقلال |
| 2 | تعميم 3 أنواع → مستقلة برتبها + Dry-run عرض العدد | ✅ `saveGlobalRulesBatch` + اختبار ذري + عدّاد |
| 3 | المدى كلمة→كلمة بالنقر مثبّت قبل الإنشاء | ✅ آلة نقر + شارة تثبيت + اختبار منطقي |
| 4 | 4 كلمات ← 4 كيانات + تراجع واحد | ✅ اختبار مخزن (`past +1` ثم `undo`) |
| 5 | الذرية: فشل جزئي لا يترك نصف مجموعة | ✅ اختبار رفض الدفعة (عنوان/نمط فاسد) |
| 6 | لا شاشة إنشاء مكررة + كل الفحوص خضراء | ✅ توحيد T3 + 529 اختبار + typecheck + build |

## قرارات الغموض (الأكثر أمانًا — موثقة)

1. **تحديد المعالج الداخلي**: مسودة مؤقتة مهيأة من تعليم المحرر الموحد،
   لا نظام تحديد ثانٍ دائم؛ المصدر الوحيد يبقى `selection`/التعليم في
   المخزن، والمسودة تُستهلك عند الإنشاء فقط.
2. **المنشئ الصرفي/النحوي الكامل**: لم يُنسخ داخل المعالج (قدرة 1000+ سطر
   بلا داعٍ للتكرار)؛ الخطوة ٦ تحرر النمط الحرفي (ضبط/نطاق/مجموعات)
   وتفتح المنشئ الكامل مزروعًا للمتقدم — توحيد الباب لا حذف القدرة.
3. **التراجع عن دفعة القواعد العامة**: الذرية مضمونة عند الإنشاء
   (`saveGlobalRulesBatch`)؛ والتراجع اللاحق عبر `deleteGlobalRulesBatch`
   (حذف الدفعة كاملة) — القواعد العامة خارج `past` المستند لأنها على
   مستوى المصحف لا الآية.
4. **المواضع «المتخطاة» في Dry-run**: تُحسب في الآية الحالية فقط (تطابق
   مع اختلاف محلي من النوع نفسه) — صادق ورخيص؛ إحصاء المصحف كله يتطلب
   فتح 6236 مستندًا وهو خارج NFR-02.
5. **سياق كل نوع**: تجاوز اختياري فوق سياق المجموعة (وليس إلزاميًا لكل
   نوع) — يبقي الخطوة ٧ سريعة للحالة الشائعة.
