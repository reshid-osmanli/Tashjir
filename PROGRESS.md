# سجل التقدم — PROGRESS

سجل بروتوكول حزم العمل: صف لكل حزمة يُحدَّث عند البدء وعند الانتهاء، مع
المعرفات المنجزة والمتبقية ليستكملها الوكيل التالي بنفس الحزمة عند الحاجة.

| الحزمة | المرحلة | الحالة | ملاحظات |
|--------|---------|--------|---------|
| 01 — نموذج v8 | PH1 | ✅ منجزة (قبل هذه الجلسة) | كيانات v8 والمخزن الموحد |
| 02 — /studio | PH1 | ✅ منجزة (قبل هذه الجلسة) | استوديو المحرك |
| 03 — التحديد الموحد والقوائم الاحترافية | PH2 | ✅ منجزة (هذه الجلسة) | التفاصيل أدناه |

---

## الحزمة 03 — التحديد الموحد والقوائم الاحترافية (PH2)

**المتطلبات:** FR-ED-01 · FR-ED-02 (+ AC-06) · NFR-01 — **الحالة: مكتملة.**

### T1 — سياق التحديد الموحد (FR-ED-02) ✅

- **مصدر حقيقة واحد:** حقل `selection` في `stores/editor-store.ts`، وكل
  الكتابات تمر من الكاتبة المركزية `selectionWrite`: تحديث الحقل + مرايا
  التوافق (`selectedVariantId`…) + `lastSelection` (آخر تحديد صالح) + طلب
  تركيز متزايد `selectionFocus { token, center, at }`.
- **الواجهة الموحدة:** `lib/editor/selection-store.ts` — `selectElement`
  (مدخل كل اللوحات)، `useSelectionFocus`، `selectionTargetSelector`،
  `isRectOutsideViewport`، `computeFocusPan` (نقية مختبرة، ≤ 300ms
  `FOCUS_BUDGET_MS`).
- **الأنواع العشرة:** كلمة، **حرف**، **موضع**، سطر، جزء، اختلاف، وجه، قاعدة،
  **وجه مركب**، **علامة وقف** (الغامق جديد في `EditorSelectionKind`؛ الأنواع
  الثلاثة الأخيرة تعمل عبر `setSelection` مع مزامنة المرايا).
- **الانتقال والتمييز (AC-06):** `TashjeerCanvas` يستهلك طلبات التركيز كلها
  (لا الاختلافات فقط كما كان): كشف العنصر بمحدد `data-*`، تحريك اللوحة إليه
  إن خرج عن الهامش أو تمركزه عند الطلب، نبضة على **الأداة نفسها** (اختلاف/
  وجه) لا سطرها كله (`activeEntry`/`pulseEntryKey` في `TashjeerFigure`)،
  وبطاقة معلومات مصغرة `SelectionFocusCard`.
- **لوحة التفاصيل:** `SelectionDetailsPanel` أعلى لوحة الخصائص: المعرف،
  الرتبة/الترتيب، الآية، الصفحة (تقريبية — انظر القرارات)، الفئة، القاعدة
  المرتبطة، اختلافات الموضع، العلاقات، الأجزاء، المصدر (محرك/محرر)، حالة
  التصحيح، وأزرار العمليات الممكنة.
- **قائمة السياق:** `SelectionContextMenu` بالزر الأيمن على عناصر اللوحة،
  أوامرها من الوحدة النقية `selection-commands.ts` (نسخ/قص/لصق/حذف/ترتيب/
  نسخ المعرف). عمليات التحرير العميقة (دمج/نقل متقدم) لحزمة 04.
- **لوحة العلاقات (مثال Line 25 الملزم):** صفوف «ترتيب الأسطر» وأطراف
  العلاقات والأجزاء قابلة للنقر = تحديد عالمي؛ وقوائم `LineSelect`/
  `FaceSelect` وأهداف الأجزاء تحدد عند التغيير — اللوحة تنتقل وتميز.
- **التنظيف الآمن:** حذف الاختلاف/الوجه/الجزء/الوقف المحدد يصفر التحديد مع
  إبقاء `lastSelection` لسلسلة رمادية في الشريط العلوي ولوحة التفاصيل.
- **اللغة البصرية الموحدة:** `--tashjeer-select` زمردي للتحديد،
  `--tashjeer-focus` كهرماني للتوجيه، صفوف `.selection-row-active` /
  `.selection-row-focus` في `globals.css` — بلا ألوان خاصة باللوحات.

### T2 — القوائم الطويلة القابلة للتمرير (FR-ED-01) ✅

- **الغلاف الموحد** `components/ui/ScrollableList.tsx`: رأس ثابت (بحث/تصفية/
  إجراءات لا تُمرَّر)، شريط تمرير رأسي مرئي دائمًا عند التجاوز
  (`scrollbar-gutter` + تنسيق `tashjeer-scroll-area`)، زرا صعود/نزول عائمان
  في طرف القائمة (RTL: يمينها) يظهران عند الحاجة — نقرة = خطوة وضغط مستمر =
  تمرير متواصل، زر «إلى أعلى القائمة» بعد تمرير بعيد، تمرير بالعجلة
  واللمس والأسهم (tabIndex + سلوك أصلي)، Scroll Into View للعنصر النشط في
  منتصف الرؤية، و`registerApi` للتمرير البرمجي.
- **التنافذ:** نواة نقية `hooks/windowed-list-core.ts` (استُخرجت من
  `useWindowedList` فلا يتفرق الحساب) + خيار **تحميل تدريجي**
  (`onNearBottom`) للصفوف المعقدة — كلاهما مسموح في NFR-01.
- **التطبيق:** قائمة الاختلافات أولًا (`VariantsPanel` — بحث فوري في الرأس
  الثابت)، ثم ترتيب الأسطر (`LineOrderEditor`، بلا تنافذ لثبات مقابض
  السحب)، فهرس القواعد المدمج (`RulesIndexDialog`)، ومستكشف القواعد في
  `/studio` (`RuleExplorer`)، مع شريط التمرير المنسق في نوافذ تحرير الأوجه
  وتتبع المواضع. العنصر 847 يُصل إليه بخطوتين (بحث ← تحديد ← تمركز في
  منتصف الرؤية) — مختبر في `windowed-list-performance.test.ts`.
- **خارج النطاق عمدًا (بند §9 من الحزمة):** قوائم `/tracking` على مستوى
  الصفحة — تُستكمل في حزمة 09؛ روابطها العميقة للمحرر تعمل (`?ayah=&variant=`).

### T3 — الاختبارات ✅

- `tests/selection-store.test.ts` (15): الكتابة الموحدة، تغيير العنصر،
  المرايا، تعدد اللوحات عبر المخزن، سلسلة السياق «الآية ← السطر ٢٥ ←
  الاختلاف ← الوجه»، التنظيف الآمن، الأنواع الموسعة، أوامر العنصر النشط.
- `tests/selection-focus.test.ts` (13): محددات الأهداف لكل نوع، كشف الخروج
  عن الرؤية بحوافه، حساب pan والتمركز، ميزانية 300ms.
- `tests/windowed-list-performance.test.ts` (11): نوافذ 2000 عنصر صحيحة
  الحشوات، تمركز الصف 1500 و847، اتجاهات التمرير، أداء (تصفية 1000 ×100 +
  120 خطوة تمرير) تحت سقف متسع.
- الاختبارات القائمة كلها خضراء: **543 ناجحًا / 2 متخطى (50 ملفًا)**،
  `typecheck` و`build` و`lint` نظيفة.

### قرارات حُسمت أثناء التنفيذ (توثيق الغموض)

1. **موقع مخزن التحديد:** الحقل `selection` في `editor-store` هو «السليس» —
   الموقع المكافئ المسموح به، لأن المحرر ولوحاته تعمل على مخزن واحد أصلًا؛
   الواجهة الموحدة في `lib/editor/selection-store.ts` هي مدخل اللوحات. لم
   يُنشأ مخزن ثانٍ تفاديًا لمصدرَي حقيقة.
2. **الصفحة في لوحة التفاصيل:** بيانات المصحف لا تحمل صفحة لكل آية (أرقام
   صفحات مصحف المدينة خارج النطاق وفق SRS_STATUS §4.1) — تُعرض «الصفحة
   (تقريبية)» من أول صفحة للسورة، وتُرقَّم بدقة عند إدخال بيانات الصفحات.
3. **قائمة السياق:** الأوامر الممكنة فعلًا اليوم هي نسخ/قص/لصق/حذف (بأنواعها)/
   ترتيب أعلى-أسفل/نسخ المعرف/فتح في الاستوديو/فك الوجه المركب (عبر لوحة
   العلاقات). عمليات الدمج والنقل المتقدم تُبنى في حزمة 04 فوق نفس واجهة
   `selectionCommands`.
4. **النبضة المستهدفة:** النقر على اختلاف داخل سطر ينبض الأداة (مدى تغليظها)
   لا السطر كله؛ تحديد الاختلاف ككل يبرز أدواته في كل أسطره (`activeEntry`).
5. **ترتيب ظهور القراء في القوائم:** لم يُمس — الترتيب من البيانات
   (`displayOrder`/`orderRank`) كما هو، وتعديل الاسم لا يعيد الترتيب
   (معيار القبول 5 يرصده اختبارات `catalog-order` القائمة).

### المتبقي على الحزم التالية (بمعرفاتها)

- حزمة 04: تنفيذ عمليات الدمج/النقل/المسدس الكامل فوق
  `selectionCommands` + التحديد المتعدد Ctrl/Shift للحذف الجماعي.
- حزمة 09: قوائم `/tracking` على مستوى الصفحة (تحميل تدريجي + بحث نصي) إن
  لزم إعادة بناء شاملة.
- (تحسيني) أرقام صفحات مصحف المدينة الدقيقة لإزالة «تقريبية» من لوحة التفاصيل.

*— نهاية سجل الحزمة 03 —*
